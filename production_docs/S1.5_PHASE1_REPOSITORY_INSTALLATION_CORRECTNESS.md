Historical implementation record.

This document reflects the project state at the named stage and may contain superseded architecture, providers, syntax, tests, or scope. Refer to README.md, technical_design.md, and active files under docs/ for current submission behavior.

---

# Stage S1.5 - Phase 1 Repository and Installation Correctness

**Stage Status:** Complete  
**Date:** July 28, 2026  
**Scope:** Documentation-link cleanup, dependency declarations, clean backend install verification, and path-containment hardening.

---

## Summary

Phase 1 corrected active machine-local documentation links, made backend package discovery/install metadata explicit, declared missing runtime and test dependencies found by import/startup audit, verified clean backend installation in a fresh Python 3.10 virtual environment, and replaced string-prefix path-containment checks with resolved-path validation.

Phase 2 was not started.

---

## Changes Made

- Replaced active machine-local file-scheme links in `AGENTS.md` with repository-relative links.
- Updated README/backend setup docs to state the tested Python policy: Python 3.10.x only for Phase 1.
- Updated backend package metadata:
  - runtime dependencies: FastAPI, Uvicorn, Pydantic, Pydantic Settings, WebSockets, Pillow, NumPy, OpenCV headless, Python Multipart;
  - dev/test dependencies: pytest, pytest-asyncio, httpx, Ruff, Mypy, msgpack, google-genai;
  - explicit setuptools package discovery for `app*` only.
- Added `app.core.path_safety.resolve_path_within`.
- Replaced upload/export path prefix checks in `ProjectService`.
- Added path-safety regression tests for child paths, traversal, sibling-prefix escape, relative paths, Windows/POSIX prefix semantics, and non-existent target paths.

---

## Validation Evidence

Fresh environment:

```text
python -m venv <fresh_venv_outside_repo>
<fresh_venv_outside_repo>\Scripts\python -m pip install --upgrade pip
<fresh_venv_outside_repo>\Scripts\python -m pip install -e backend[dev]
<fresh_venv_outside_repo>\Scripts\python --version
```

Result:

```text
Python 3.10.3
install passed after package discovery and python-multipart fixes
```

Backend startup smoke:

```text
<fresh_venv_outside_repo>\Scripts\python -c "from app.main import app; print(app.title); print(len(app.routes)); print('startup import ok')"
```

Result:

```text
InterfaceForge Backend
7
startup import ok
```

Tests and checks:

```text
<fresh_venv_outside_repo>\Scripts\python -m pytest tests\test_path_safety.py
<fresh_venv_outside_repo>\Scripts\python -m pytest tests
<fresh_venv_outside_repo>\Scripts\python -m ruff check app tests
<fresh_venv_outside_repo>\Scripts\python -m ruff format --check app tests
<fresh_venv_outside_repo>\Scripts\python -m mypy --explicit-package-bases app
npm test
npm run build
python scripts\audit_repository.py
git diff --check
```

Results:

```text
Path-safety tests: 8 passed
Backend tests: 299 passed, 1 warning
Ruff check: passed
Ruff format check: passed
Mypy: passed
Frontend tests: 81 passed
Frontend build: passed
Repository audit: passed
git diff --check: passed
```

---

## Notes and Deviations

- The technical design recommends Python 3.12, but this machine only has Python 3.10 and 3.9 available. Phase 1 documentation and configuration now claim only Python 3.10.x support because that is what was verified in a fresh environment.
- Live Zoo/Gemini provider verification was not performed and remains credential-dependent. Local startup used mock-safe provider defaults.
- A wider repository local-path search still finds machine-local file-scheme links in archived `production_docs/S*` historical reports and a hardcoded local path in `scripts/compare_s10_5c_routes.py`. Those were not edited because Phase 1 requested active documentation cleanup and explicitly avoided archived historical reports unless presented as current instructions.

---

## Stage Close

Current stage is ready to close for Phase 1. Recommended next stage scope: begin Phase 2 only after product-owner review of this Phase 1 report and current git diff.
