Historical implementation record.

This document reflects the project state at the named stage and may contain superseded architecture, providers, syntax, tests, or scope. Refer to README.md, technical_design.md, and active files under docs/ for current submission behavior.

---

# Stage S10.7 - Focused UI Correction Pass

**Stage Status:** Ready for product-owner visual QA  
**Date:** July 28, 2026  
**Scope:** Annotated landing-page correction pass covering header branding, provider mode selection, readable project names, creator help, truthful service status, hero icon, and example placeholders.

## Work Completed

- Replaced the header full-logo/duplicate-name presentation with the compact `InterfaceForge_logo_in.svg` icon plus separate `Wordmark` text.
- Added project-scoped `provider_mode` metadata and backend endpoints for reading/updating provider mode.
- Added truthful provider status metadata so the frontend renders Live only when backend credentials allow live Zoo providers.
- Added default readable project names using `Adapter N` while preserving `project_id` for API and database use.
- Reworked the Help panel around creator attribution for Joravar Singh with a safe external portfolio link.
- Updated the status bubble to show Live with a reduced-motion-aware pulse only in effective live mode, and Mock / Offline otherwise.
- Added the compact InterfaceForge icon above the landing hero heading.
- Added accessible image-placeholder regions to both example cards.

## Provider Behavior

- Mock mode remains the default for new projects.
- Live mode requires backend `ZOO_API_TOKEN`; unavailable Live requests return `IF-PROVIDER-409` and do not change project state.
- Effective mode is stored on the backend project record and is used by generation, export, and Agent provider selection.
- Analysis uses Gemini only when Live mode is effective and Gemini credentials are available; otherwise it remains mock and is reported as such in provider status.

## Validation Evidence

- `.\venv\Scripts\python -m pytest backend/tests/test_provider_mode.py backend/tests/test_generation.py backend/tests/test_export.py backend/tests/test_agent_bounded_revisions.py` - 47 passed, 1 warning.
- `npm test` - 90 passed across 11 frontend test files.
- `npm run build` - passed.
- `npm run lint` - passed.
- `git diff --check` - passed with CRLF normalization warnings only.
- `python scripts/audit_repository.py` - passed.

## Deviations / Risks

- Existing working tree contained unrelated uncommitted changes before this pass; they were preserved.
- Visual QA is still required for final alignment at desktop and narrow widths.
- Live-mode end-to-end verification requires real backend credentials and network access.

## Stage Close Recommendation

Ready to close after product-owner visual QA of the annotated UI corrections and live-credential manual checks.